name: Sync Todo

on:
  schedule:
    - cron: '0 2 * * *' # daily at 02:00 UTC
  issues:
    types: [opened, edited, closed, labeled, unlabeled]

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run todo sync (placeholder)
        run: |
          echo "This job should call a script to update todo.md from issues"
